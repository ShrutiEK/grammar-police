# OpenAI integration

Provider-specific transcription and assessment code belongs here. Domain modules
must access it through narrow interfaces and validate every response before use.

The browser must never receive `OPENAI_API_KEY`.
